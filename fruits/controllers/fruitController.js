const express = require ('express');

// next we set up the ROUTE
const router = express.Router();

// require our Model (should be Caps)

//router.destroy('/fruits/:index/delete', (req,res) =>{
	//Fruits.splice(******!!!******YOU HAD IT!)
//})

//create query params to be sent by client
	







module.exports = router;
//creating a MODULE thatsw just going to deal with Fruit